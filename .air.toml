root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o tmp/api cmd/api/main.go"
  bin = "tmp/api"
  full_bin = "tmp/api"
  include_ext = ["go"]
  exclude_dir = ["tmp", "vendor"]
  delay = 1000 # ms

[log]
  time = true

[color]
  main = "yellow"
  watcher = "cyan"
  build = "green"
  runner = "magenta"